SET( TARGET_BASE_NAME test1 )

SET(SRCS test1.cpp)
SET (INCS)

ADD_EXECUTABLE(test1 WIN32 ${SRCS} ${INCS})

TARGET_LINK_LIBRARIES ( test1
  ${FFMPEG_LIBRARIES}

  ${Poco_LIBRARIES}

  ${CMAKE_THREAD_LIBS_INIT}
  ${CMAKE_DL_LIBS}
)
